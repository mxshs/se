//this file is generated by program(drogon_ctl) automatically,don't modify it!
#include "doc_page.h"
#include <drogon/utils/OStringStream.h>
#include <string>
#include <map>
#include <vector>
#include <set>
#include <iostream>
#include <unordered_map>
#include <unordered_set>
#include <algorithm>
#include <list>
#include <deque>
#include <queue>
using namespace drogon;
std::string doc_page::genText(const DrTemplateData& doc_page_view_data)
{
	drogon::OStringStream doc_page_tmp_stream;
	std::string layoutName{"base_layout"};
 auto doc=doc_page_view_data.get<char*>("doc");
	doc_page_tmp_stream << "<md-block>\n";
	doc_page_tmp_stream << "    ";
doc_page_tmp_stream<<doc;
	doc_page_tmp_stream << "</md-block>\n";
if(layoutName.empty())
{
std::string ret{std::move(doc_page_tmp_stream.str())};
return ret;
}else
{
auto templ = DrTemplateBase::newTemplate(layoutName);
if(!templ) return "";
HttpViewData data = doc_page_view_data;
auto str = std::move(doc_page_tmp_stream.str());
if(!str.empty() && str[str.length()-1] == '\n') str.resize(str.length()-1);
data[""] = std::move(str);
return templ->genText(data);
}
}
