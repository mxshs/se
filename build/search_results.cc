//this file is generated by program(drogon_ctl) automatically,don't modify it!
#include "search_results.h"
#include <drogon/utils/OStringStream.h>
#include <string>
#include <map>
#include <vector>
#include <set>
#include <iostream>
#include <unordered_map>
#include <unordered_set>
#include <algorithm>
#include <list>
#include <deque>
#include <queue>
using namespace drogon;
std::string search_results::genText(const DrTemplateData& search_results_view_data)
{
	drogon::OStringStream search_results_tmp_stream;
	std::string layoutName{""};
 auto result=search_results_view_data.get<std::vector<std::pair<std::string, float>>>("result");
 auto shortText=search_results_view_data.get<std::vector<std::string>>("shortText");
	search_results_tmp_stream << "<div>\n";
	search_results_tmp_stream << "    ";
 for (int idx = 0; idx < result.size(); idx++) {
	search_results_tmp_stream << "        <p>\n";
	search_results_tmp_stream << "            <a href=\"";
search_results_tmp_stream<<result[idx].first;
	search_results_tmp_stream << "\">";
search_results_tmp_stream<<result[idx].first;
	search_results_tmp_stream << "</a>\n";
	search_results_tmp_stream << "        </p>\n";
	search_results_tmp_stream << "        <p>";
search_results_tmp_stream<<shortText[idx];
	search_results_tmp_stream << "</p>\n";
	search_results_tmp_stream << "    ";
}
	search_results_tmp_stream << "</div>\n";
if(layoutName.empty())
{
std::string ret{std::move(search_results_tmp_stream.str())};
return ret;
}else
{
auto templ = DrTemplateBase::newTemplate(layoutName);
if(!templ) return "";
HttpViewData data = search_results_view_data;
auto str = std::move(search_results_tmp_stream.str());
if(!str.empty() && str[str.length()-1] == '\n') str.resize(str.length()-1);
data[""] = std::move(str);
return templ->genText(data);
}
}
