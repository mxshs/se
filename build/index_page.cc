//this file is generated by program(drogon_ctl) automatically,don't modify it!
#include "index_page.h"
#include <drogon/utils/OStringStream.h>
#include <string>
#include <map>
#include <vector>
#include <set>
#include <iostream>
#include <unordered_map>
#include <unordered_set>
#include <algorithm>
#include <list>
#include <deque>
#include <queue>
using namespace drogon;
std::string index_page::genText(const DrTemplateData& index_page_view_data)
{
	drogon::OStringStream index_page_tmp_stream;
	std::string layoutName{"base_layout"};
	index_page_tmp_stream << "<div>\n";
	index_page_tmp_stream << "    <form hx-get=\"/search\" hx-target=\"#result\">\n";
	index_page_tmp_stream << "        <input type=\"text\" name=\"search\">\n";
	index_page_tmp_stream << "        <input type=\"submit\" value=\"Search\">\n";
	index_page_tmp_stream << "    </form>\n";
	index_page_tmp_stream << "    <div id=\"result\"></div>\n";
	index_page_tmp_stream << "</div>\n";
if(layoutName.empty())
{
std::string ret{std::move(index_page_tmp_stream.str())};
return ret;
}else
{
auto templ = DrTemplateBase::newTemplate(layoutName);
if(!templ) return "";
HttpViewData data = index_page_view_data;
auto str = std::move(index_page_tmp_stream.str());
if(!str.empty() && str[str.length()-1] == '\n') str.resize(str.length()-1);
data[""] = std::move(str);
return templ->genText(data);
}
}
